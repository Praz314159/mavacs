[package]
name = "mavacs_plus"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
sha3 = "0.10.8"
bincode = "1.3"
hash = "0.3.0"
dhat = { version = "0.3", optional = true }

[dev-dependencies]

[profile.release]
debug = true

[features]
dhat-heap = ["dhat"]
